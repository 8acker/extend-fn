{
  "name": "extend-fn",
  "version": "0.0.1",
  "description": "Extend json object with other objects that could also have functions as prperties",
  "main": "index.js",
  "dependencies": {
    "json-fn": "^0.60.0",
    "object-assign": "^4.0.1"
  },
  "devDependencies": {
    "mocha": "^2.3.3"
  },
  "scripts": {
    "test": "mocha --reporter spec test/*.js"
  }
}
